package ru.skypro.homework.dto;

/**
 * Enum `Role` представляет собой перечисление с двумя значениями: `USER` и `ADMIN`.
 */
public enum Role {
    //Это простой перечисляемый тип данных, который используется для определения роли пользователя в системе.
    // В данном случае `Role` может быть либо `USER`, либо `ADMIN`.
    USER, ADMIN
}
